s1 = CREATE TABLE PERSON \
( \
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL \
, NAME VARCHAR(100) NOT NULL  \
);

s2 = CREATE SEQUENCE HIBERNATE_SEQUENCE AS BIGINT START WITH 100 INCREMENT BY 1;

s3 = CREATE TABLE CONTACT \
( \
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL \
, PERSON_ID BIGINT NOT NULL  \
, ADDRESS VARCHAR(100) NOT NULL  \
, PHONE_NUMBER VARCHAR(100) \
);

s4 = ALTER TABLE CONTACT ADD CONSTRAINT FK_CONTACT_PERSON \
	FOREIGN KEY (PERSON_ID) REFERENCES PERSON (ID) ON DELETE CASCADE \
;

s5 = CREATE TABLE MEDIA \
( \
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL \
, TITLE VARCHAR(100) NOT NULL  \
);

s6 = CREATE TABLE MOVIE \
( \
  MEDIA_ID BIGINT NOT NULL  \
, URLIMDB VARCHAR(100) NOT NULL \
, PLAYLENGTH INT NOT NULL \
);

s7 = CREATE TABLE BOOK \
( \
  MEDIA_ID BIGINT NOT NULL  \
, ISBN VARCHAR(100) NOT NULL \
);

s8 = ALTER TABLE MOVIE ADD CONSTRAINT FK_MOVIE_MEDIA \
	FOREIGN KEY (MEDIA_ID) REFERENCES MEDIA (ID) ON DELETE CASCADE \
;

s9 = ALTER TABLE BOOK ADD CONSTRAINT FK_BOOK_MEDIA \
	FOREIGN KEY (MEDIA_ID) REFERENCES MEDIA (ID) ON DELETE CASCADE \
;

s10 = CREATE TABLE PERSON_LIBRARY \
( \
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL \
, PERSON_ID BIGINT NOT NULL  \
, MEDIA_ID BIGINT NOT NULL  \
);

s11 = ALTER TABLE PERSON_LIBRARY ADD CONSTRAINT FK_PERSON_LIBRARY_1 \
	FOREIGN KEY (PERSON_ID) REFERENCES PERSON (ID) \
;

s11 = ALTER TABLE PERSON_LIBRARY ADD CONSTRAINT FK_PERSON_LIBRARY_2 \
	FOREIGN KEY (MEDIA_ID) REFERENCES MEDIA (ID) \
;

