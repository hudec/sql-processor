package org.sample.dao.auth {

  #CRUD(AuthRole)
  #Query(AuthRole)
  dao AuthRoleDao {
  }

  #CRUD(AuthUser)
  #Query(AuthUser)
  dao AuthUserDao {
  }

  #CRUD(UserRole)
  #Query(UserRole)
  dao UserRoleDao {
  }

  #CRUD(UserSession)
  #Query(UserSession)
  dao UserSessionDao {
  }
}
