package org.sample.dao.person {

  #CRUD(Contact)
  #Query(Contact)
  dao ContactDao {
  }

  #CRUD(Country)
  #Query(Country)
  dao CountryDao {
  }

  #CRUD(Person)
  #Query(Person)
  dao PersonDao {
  }

  #CRUD(Relative)
  #Query(Relative)
  dao RelativeDao {
  }
}
